apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ott-ingress
spec:
  rules:
  - host: ott.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80

